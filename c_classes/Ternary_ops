#include <stdio.h>
#include <stdlib.h>
#include <conio.h>

/**
* main - Checks a number and determines if tis positive or negative
*
* Return: Always 0 (Success)
*/

int main() {

   int n;

   printf("Input number: ");
   scanf("%d", &n);
(n%2 == 0) ? printf("The number %d is even", n) : printf("The number %d is odd", n);

    return 0;
}
